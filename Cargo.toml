[package]
name = "depot"
version = "0.1.0"
edition = "2024"

[dependencies]
sha1 = "0.10"
flate2 = { version = "1", features = ["rust_backend"], default-features = false }
memmap = "0.7.0"
similar = { version = "2.0", features = ["inline"], default-features = false }